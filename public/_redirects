# Catch-all: serve index.html for all paths so the Vue SPA router handles routing.
# Required on Cloudflare Pages (and Netlify) – without this, a hard refresh on any
# path other than / returns a 404 from the CDN.
/*    /index.html   200
